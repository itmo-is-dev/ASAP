@using ITMO.Dev.ASAP.WebUI.Abstractions.Authorization
@using ITMO.Dev.ASAP.WebUI.Abstractions.Extensions
@inject ICurrentUser CurrentUser

<DropdownTabTitleComponent OnClick="OnSelectedTabChanged" Name="@Key">
    <ButtonContent>
        Assignments
    </ButtonContent>

    <DropdownContent>
        @if (CurrentUser.HasModeratorAccess())
        {
            <DropdownItem>
                <CreateButton Clicked="OnClicked"
                              Background="Background.Transparent"
                              TextColor="TextColor.Body"
                              FitContent="false"/>
            </DropdownItem>
        }
    </DropdownContent>

</DropdownTabTitleComponent>

@code {

        public const string Key = "assignments";

    [Parameter]
    public EventCallback<string> OnSelectedTabChanged { get; set; }

    [Parameter]
    public EventCallback OnClicked { get; set; }

}